// Array: A fixed-size, contiguous block of memory used to store elements of the same data type.

// Arrays are fixed in size and always use contiguous memory.
// The memory for an array is allocated when the array is declared, and it cannot grow or shrink during runtime.




// fixed size array (static size)
// continuous memory allocation

//below are stack allocated
// int arr[100];
// int arr[n];
// int arr = {1, 2, 3, 4};

// // below is heap allocate             // dynamic allocation
// int *arr = new int[n]


