Boto3/Botocore

boto3 is the official AWS SDK for Python. It lets your Python code call AWS APIs in a structured way instead of hand-crafting HTTP requests.

botocore is the lower-level library boto3 is built on; it handles request signing, retries, and parsing responses.